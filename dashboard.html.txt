<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - SPX Predictor</title>
    <meta name="description" content="SPX Price Prediction Dashboard">
    
    <!-- Chart.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.9.1/chart.min.js"></script>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            line-height: 1.6;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 15px 30px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .navbar .logo {
            font-size: 1.5em;
            font-weight: 700;
            color: #2c3e50;
        }

        .navbar .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .navbar .user-name {
            color: #34495e;
            font-weight: 500;
        }

        .navbar .logout-btn {
            background: #e74c3c;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .navbar .logout-btn:hover {
            background: #c0392b;
            transform: translateY(-1px);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .dashboard-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .dashboard-header h1 {
            color: #2c3e50;
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .dashboard-header p {
            color: #7f8c8d;
            font-size: 1.1em;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-icon {
            font-size: 2.5em;
            margin-bottom: 15px;
        }

        .stat-value {
            font-size: 2em;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .stat-label {
            color: #7f8c8d;
            font-size: 0.9em;
        }

        .prediction-section {
            margin-bottom: 40px;
        }

        .section-title {
            color: #2c3e50;
            font-size: 1.8em;
            margin-bottom: 20px;
            text-align: center;
        }

        .prediction-form {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #2c3e50;
            font-weight: 500;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #3498db;
        }

        .btn {
            background: linear-gradient(45deg, #3498db, #2980b9);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }

        .prediction-result {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
        }

        .chart-container {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        .iron-condor-section {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .strikes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .strike-card {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
        }

        .strike-type {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .strike-value {
            font-size: 1.5em;
            color: #3498db;
            font-weight: 700;
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .form-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">📈 SPX Predictor</div>
        <div class="user-info">
            <span class="user-name">Demo User</span>
            <button class="logout-btn" onclick="logout()">Logout</button>
        </div>
    </nav>

    <div class="container">
        <div class="dashboard-header">
            <h1>SPX Trading Dashboard</h1>
            <p>Advanced machine learning predictions for iron condor strategies</p>
        </div>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon">🎯</div>
                <div class="stat-value" id="accuracy">78.3%</div>
                <div class="stat-label">Prediction Accuracy</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">💰</div>
                <div class="stat-value" id="profit">+$12,450</div>
                <div class="stat-label">Monthly P&L</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📊</div>
                <div class="stat-value" id="trades">24</div>
                <div class="stat-label">Successful Trades</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">⚡</div>
                <div class="stat-value" id="winRate">72%</div>
                <div class="stat-label">Win Rate</div>
            </div>
        </div>

        <div class="prediction-section">
            <h2 class="section-title">Generate SPX Prediction</h2>
            <div class="prediction-form">
                <div class="form-grid">
                    <div class="form-group">
                        <label for="currentPrice">Current SPX Price</label>
                        <input type="number" id="currentPrice" value="4850" step="0.01">
                    </div>
                    <div class="form-group">
                        <label for="daysToExpiry">Days to Expiry</label>
                        <select id="daysToExpiry">
                            <option value="1">1 Day</option>
                            <option value="2">2 Days</option>
                            <option value="3">3 Days</option>
                            <option value="5">5 Days</option>
                            <option value="7" selected>1 Week</option>
                            <option value="14">2 Weeks</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="volatility">Implied Volatility (%)</label>
                        <input type="number" id="volatility" value="18.5" step="0.1">
                    </div>
                    <div class="form-group">
                        <label for="strategy">Strategy Type</label>
                        <select id="strategy">
                            <option value="iron-condor" selected>Iron Condor</option>
                            <option value="butterfly">Butterfly</option>
                            <option value="straddle">Straddle</option>
                        </select>
                    </div>
                </div>
                <button class="btn" onclick="generatePrediction()">Generate Prediction</button>
            </div>

            <div class="prediction-result" id="predictionResult" style="display: none;">
                <h3>Prediction Results</h3>
                <p id="predictionText"></p>
            </div>
        </div>

        <div class="chart-container">
            <h2 class="section-title">SPX Price Movement</h2>
            <canvas id="priceChart" width="400" height="200"></canvas>
        </div>

        <div class="iron-condor-section">
            <h2 class="section-title">Recommended Iron Condor Strikes</h2>
            <div class="strikes-grid">
                <div class="strike-card">
                    <div class="strike-type">Put Strike (Short)</div>
                    <div class="strike-value" id="putShort">4800</div>
                </div>
                <div class="strike-card">
                    <div class="strike-type">Put Strike (Long)</div>
                    <div class="strike-value" id="putLong">4750</div>
                </div>
                <div class="strike-card">
                    <div class="strike-type">Call Strike (Short)</div>
                    <div class="strike-value" id="callShort">4900</div>
                </div>
                <div class="strike-card">
                    <div class="strike-type">Call Strike (Long)</div>
                    <div class="strike-value" id="callLong">4950</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Initialize chart
        const ctx = document.getElementById('priceChart').getContext('2d');
        const priceChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri'],
                datasets: [{
                    label: 'SPX Price',
                    data: [4820, 4835, 4842, 4856, 4850],
                    borderColor: '#3498db',
                    backgroundColor: 'rgba(52, 152, 219, 0.1)',
                    borderWidth: 3,
                    fill: true,
                    tension: 0.4
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        display: false
                    }
                },
                scales: {
                    y: {
                        beginAtZero: false,
                        min: 4800,
                        max: 4900
                    }
                }
            }
        });

        function generatePrediction() {
            const currentPrice = parseFloat(document.getElementById('currentPrice').value);
            const days = parseInt(document.getElementById('daysToExpiry').value);
            const volatility = parseFloat(document.getElementById('volatility').value);
            
            // Simple prediction simulation
            const randomFactor = (Math.random() - 0.5) * 0.02;
            const volatilityFactor = (volatility / 100) * Math.sqrt(days / 252);
            const predictedChange = randomFactor + volatilityFactor;
            const predictedPrice = currentPrice * (1 + predictedChange);
            
            // Update strikes based on prediction
            updateIronCondorStrikes(predictedPrice);
            
            // Show prediction result
            const resultDiv = document.getElementById('predictionResult');
            const predictionText = document.getElementById('predictionText');
            
            predictionText.innerHTML = `
                <strong>Predicted SPX Price in ${days} days: $${predictedPrice.toFixed(2)}</strong><br>
                Expected change: ${(predictedChange * 100).toFixed(2)}%<br>
                Confidence: ${(65 + Math.random() * 20).toFixed(1)}%
            `;
            
            resultDiv.style.display = 'block';
        }

        function updateIronCondorStrikes(predictedPrice) {
            const spread = 50;
            const buffer = 25;
            
            document.getElementById('putShort').textContent = Math.round(predictedPrice - buffer);
            document.getElementById('putLong').textContent = Math.round(predictedPrice - buffer - spread);
            document.getElementById('callShort').textContent = Math.round(predictedPrice + buffer);
            document.getElementById('callLong').textContent = Math.round(predictedPrice + buffer + spread);
        }

        function logout() {
            window.location.href = 'index.html';
        }

        // Initialize with current data
        document.addEventListener('DOMContentLoaded', function() {
            generatePrediction();
        });
    </script>
</body>
</html>